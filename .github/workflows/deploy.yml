name: 🚀 Deploy Backend to cPanel

on:
  push:
    branches: [main]
    paths:
      - '**'
  workflow_dispatch:

env:
  NODE_VERSION: "20"
  FTP_DIR: "public_html/myapp"   # 👈 MANUAL FTP DIRECTORY
  FTP_PORT: "21"                 # 👈 Change if your FTP server uses another port

jobs:
  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest
    environment:
      name: production
      url: https://benzflex.com

    steps:
      # =========================================
      # Step 1: Checkout Code
      # =========================================
      - name: 🚚 Checkout code
        uses: actions/checkout@v4

      # =========================================
      # Step 2: Debug Repo Structure (for safety)
      # =========================================
      - name: 🧭 Debug directory structure
        run: |
          echo "Current directory: $(pwd)"
          echo "Contents of repo root:"
          ls -la | head -50

      # =========================================
      # Step 3: Detect Dependency Manager
      # =========================================
      - name: 🔍 Detect dependency manager
        run: |
          if [ -f "package-lock.json" ]; then
            echo "📦 Detected npm (package-lock.json found)"
          elif [ -f "yarn.lock" ]; then
            echo "📦 Detected Yarn (yarn.lock found)"
          else
            echo "⚠️  No lock file found — caching disabled"
          fi

      # =========================================
      # Step 4: Setup Node.js with Auto Cache
      # =========================================
      - name: ⚙️ Setup Node.js (auto cache detection)
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: ${{ hashFiles('package-lock.json') != '' && 'npm' || hashFiles('yarn.lock') != '' && 'yarn' || '' }}
          cache-dependency-path: |
            package-lock.json
            yarn.lock

      # =========================================
      # Step 5: Verify FTP Secrets
      # =========================================
      - name: 🔐 Verify FTP Secrets
        run: |
          set -e
          if [ -z "${{ secrets.FTP_SERVER }}" ] || [ -z "${{ secrets.FTP_USERNAME }}" ] || [ -z "${{ secrets.FTP_PASSWORD }}" ]; then
            echo "::error::Missing required FTP secrets!"
            echo ""
            echo "Required GitHub Secrets:"
            echo "  - FTP_SERVER"
            echo "  - FTP_USERNAME"
            echo "  - FTP_PASSWORD"
            exit 1
          fi
          
          echo "✅ FTP secrets verified"
          echo "FTP_SERVER=${{ secrets.FTP_SERVER }}" >> $GITHUB_ENV
          echo "FTP_USERNAME=${{ secrets.FTP_USERNAME }}" >> $GITHUB_ENV
          echo "FTP_PASSWORD=${{ secrets.FTP_PASSWORD }}" >> $GITHUB_ENV
          echo "FTP_DIR=${{ env.FTP_DIR }}" >> $GITHUB_ENV
          echo "FTP_PO_
