name: 🚀 Deploy Backend to cPanel

on:
  push:
    branches: [main]
    paths:
      - 'backend/**'
      - '.github/workflows/deploy.yml'
  workflow_dispatch:

env:
  NODE_VERSION: "20"

jobs:
  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest
    environment:
      name: production
      url: https://benzflex.com

    defaults:
      run:
        working-directory: backend

    steps:
      # =========================================
      # Step 1: Checkout Code
      # =========================================
      - name: 🚚 Checkout code
        uses: actions/checkout@v4

      # =========================================
      # Step 2: Detect Dependency Manager
      # =========================================
      - name: 🔍 Detect dependency manager
        run: |
          if [ -f "backend/package-lock.json" ]; then
            echo "📦 Detected npm (package-lock.json found)"
          elif [ -f "backend/yarn.lock" ]; then
            echo "📦 Detected Yarn (yarn.lock found)"
          else
            echo "⚠️  No lock file found — caching disabled"
          fi

      # =========================================
      # Step 3: Setup Node.js with Auto Cache
      # =========================================
      - name: ⚙️ Setup Node.js (auto cache detection)
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: ${{ hashFiles('backend/package-lock.json') != '' && 'npm' || hashFiles('backend/yarn.lock') != '' && 'yarn' || '' }}
          cache-dependency-path: |
            backend/package-lock.json
            backend/yarn.lock

      # =========================================
      # Step 4: Verify FTP Secrets
      # =========================================
      - name: 🔐 Verify FTP Secrets
        id: verify-secrets
        run: |
          set -e
          if [ -z "${{ secrets.FTP_SERVER }}" ] || [ -z "${{ secrets.FTP_USERNAME }}" ] || [ -z "${{ secrets.FTP_PASSWORD }}" ] || [ -z "${{ secrets.FTP_DIR }}" ]; then
            echo "::error::Missing required FTP secrets!"
            echo ""
            echo "Required GitHub Secrets:"
            echo "  - FTP_SERVER"
            echo "  - FTP_USERNAME"
            echo "  - FTP_PASSWORD"
            echo "  - FTP_DIR (example: public_html/myapp)"
            exit 1
          fi

          echo "✅ FTP secrets verified"
          echo "FTP_SERVER=${{ secrets.FTP_SERVER }}" >> $GITHUB_ENV
          echo "FTP_USERNAME=${{ secrets.FTP_USERNAME }}" >> $GITHUB_ENV
          echo "FTP_PASSWORD=${{ secrets.FTP_PASSWORD }}" >> $GITHUB_ENV
          echo "FTP_DIR=${{ secrets.FTP_DIR }}" >> $GITHUB_ENV
          echo "FTP_PORT=${{ secrets.FTP_PORT || '21' }}" >> $GITHUB_ENV

      # =========================================
      # Step 5: Install Dependencies for Scripts
      # =========================================
      - name: 📦 Install basic-ftp library
        run: npm install basic-ftp --no-save --legacy-peer-deps

      # =========================================
      # Step 6: Check Remote Disk Space
      # =========================================
      - name: 💾 Check remote disk space
        id: check-disk-space
        run: node .github/scripts/checkDiskSpace.js
        env:
          FTP_SERVER: ${{ secrets.FTP_SERVER }}
          FTP_USERNAME: ${{ secrets.FTP_USERNAME }}
          FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
          FTP_DIR: ${{ secrets.FTP_DIR }}
          FTP_PORT: ${{ secrets.FTP_PORT || '21' }}

      # =========================================
      # Step 7: Verify Remote Directories
      # =========================================
      - name: 🧱 Verify remote directories
        id: verify-dirs
        run: node .github/scripts/checkFtpDirs.js
        env:
          FTP_SERVER: ${{ secrets.FTP_SERVER }}
          FTP_USERNAME: ${{ secrets.FTP_USERNAME }}
          FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
          FTP_DIR: ${{ secrets.FTP_DIR }}
          FTP_PORT: ${{ secrets.FTP_PORT || '21' }}

      # =========================================
      # Step 8: Clean Old Files (Optional Pre-Cleanup)
      # =========================================
      - name: 🧽 Clean old files (safe cleanup)
        id: cleanup-old-files
        continue-on-error: true
        run: node .github/scripts/cleanupOldFiles.js
        env:
          FTP_SERVER: ${{ secrets.FTP_SERVER }}
          FTP_USERNAME: ${{ secrets.FTP_USERNAME }}
          FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
          FTP_DIR: ${{ secrets.FTP_DIR }}
          FTP_PORT: ${{ secrets.FTP_PORT || '21' }}

      # =========================================
      # Step 9: Prepare Deployment Package
      # =========================================
      - name: 🧹 Prepare deployment package
        id: prepare-package
        run: |
          set -e
          rm -rf deployment
          mkdir -p deployment

          rsync -av \
            --exclude='node_modules' \
            --exclude='.git' \
            --exclude='.github' \
            --exclude='logs' \
            --exclude='*.log' \
            --exclude='*.env' \
            ./ ./deployment/

          cp start.sh deployment/start.sh
          chmod +x deployment/start.sh

          if [ ! -f "deployment/server.js" ] || [ ! -f "deployment/app.js" ] || [ ! -f "deployment/package.json" ]; then
            echo "::error::Critical files missing!"
            exit 1
          fi
          
          echo "✅ Deployment package ready"

      # =========================================
      # Step 10: Deploy via FTP
      # =========================================
      - name: 📤 Deploy to cPanel via FTP
        id: ftp-deploy
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          port: ${{ secrets.FTP_PORT || '21' }}
          local-dir: ./backend/deployment/
          server-dir: ${{ secrets.FTP_DIR }}
          exclude: |
            node_modules/**
            .git/**
            .github/**
            logs/**
            *.log
            *.env
          dangerous-clean-slate: false
          log-level: verbose
          timeout: 120000

      # =========================================
      # Step 11: Deployment Summary
      # =========================================
      - name: 📋 Deployment Summary
        if: success()
        run: |
          echo "🎉 Deployment completed successfully!"
          echo "🌐 Deployed to: ${{ secrets.FTP_DIR }}"
          echo ""
          echo "Next steps:"
          echo "1️⃣ npm install --production --legacy-peer-deps"
          echo "2️⃣ Set environment variables in cPanel"
          echo "3️⃣ Set startup file to: start.sh"
          echo "4️⃣ Restart Node.js app in cPanel"

      # =========================================
      # Step 12: Error Analysis
      # =========================================
      - name: 🔍 Error Analysis
        if: failure()
        run: |
          echo "❌ Deployment failed"
          echo ""
          echo "Check which step failed:"
          echo "  - Disk space check: ${{ steps.check-disk-space.outcome }}"
          echo "  - Verify directories: ${{ steps.verify-dirs.outcome }}"
          echo "  - Clean old files: ${{ steps.cleanup-old-files.outcome }}"
          echo "  - Prepare package: ${{ steps.prepare-package.outcome }}"
          echo "  - FTP deploy: ${{ steps.ftp-deploy.outcome }}"
          echo ""
          echo "Common issues:"
          echo "  - 552 = Disk full (clean up server)"
          echo "  - 553 = Directory not found"
          echo "  - 530 = Authentication failed"
          echo "  - Check GitHub Secrets are set correctly"
